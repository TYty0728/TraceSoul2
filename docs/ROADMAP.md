# 接下来能做（ROADMAP）

按优先级排列；✅ = 已完成。

**2026-08-27：** 插件三层（内核 / 平台 / 器官）已落地；QQ 说说与签名能发能看，空闲会按日限抽一件生活事；识图走多模态槽；game.session 是游戏身体。下面「一」是真聊磨合，不是再补骨架。

## 已落地（不再列进待做）

- ✅ 插件三层与休眠：平台不在则器官休眠；控制台按平台分组。
- ✅ QQ 说说：NapCat 自动取 Cookie；假失败不重发；可读她/自己的说说。
- ✅ QQ 签名/在线状态。
- ✅ 空闲生活抽签（发说说 / 看说说 / 改心情 / 歇着）。
- ✅ 识图多模态槽：看见的结果进这一拍，心智不再对着 `[图片]` 脑补。
- ✅ 夜间余烬：日终复盘后、后半夜窗口、有当天相处才开口。
- ✅ 日构建按墙钟补跑（睡过 04:00 也会追）。
- ✅ game.session 平台 + 星露谷安装/桥接。
- ✅ ChatCheck 内核与插件回归（不连真 API）。

## 一、优化与修复（短期）

- [ ] **表情去重**：移植老 smartemoji 的「近期不重复」短期记忆（同一情绪换着发，不总发同一张）。
- [ ] **表情阈值/候选**：语义兜底阈值与标签匹配权重微调；候选多于一张时引入随机（同分候选随机选）。
- [ ] **消息组装细节**：多个表情连续追加的顺序；文字+图片+语音混排规则；群聊里 @ 与回复（reply 段）的组合。
- [ ] **TTS/生图实测磨合**：音色/模板/超时；生图多尺寸与场景模板仍可再磨。
- [ ] **稳定性**：
  - 宿主重启时 NapCat 重连窗口（30s）期间的消息提示；
  - LLM 结构化输出连续失败的自动重试/降级；
  - Cookie 过期自愈（说说）。
- [ ] **控制台细节**：回合结果里图片路径改成可点击预览；长路径截断；表情包库缩略图浏览页。
- [ ] **多会话**：QQ 收件箱目前单会话路由（回复最后说话的人）；群聊多群、私聊多人并行会话的路由表。
- [ ] **Q版表情库扩充**：往 `plugins\qq-sticker\emojis\<角色ID>\` 加图 + `emoji_index.json` 加条目。

## 二、更多感官/平台（外部插件包模式）

- [ ] **语音转文字**（老 `speech_reco`）：QQ 语音消息 → 识别文本进 Brain。
- [ ] **其它平台适配器**：Discord / Telegram 等，按 OneBot 模式各写一个适配器 + 插件包。
- [ ] **星露谷 / 通用游戏物理拆包**：概念上已是 game.session 的器官，源码仍在同一插件包内。
- [ ] **`builtin.dialogue` 内核残余拆分**：console 已是平台身份，对话面职责再归位。
- [ ] **能力分级命名（暂缓）**：等真有第二个身体（机器人/摄像头）再给「本地能干多少活」正式分档命名。见 [定位文档](PLATFORM_SENSORY_POSITIONING.md)。
- [ ] **动态状态降级（暂缓）**：身体随时说实话——被挡/带宽掉/算力紧时实时降级；现在只有「有/没有」。
- [ ] **安全强制项（暂缓）**：保命开关（急停等）不许身体说「没有」；等有身体再实现。

## 三、记忆深化

- [ ] 浸染质量抽查（细节条目的第一人称一致性）。
- [ ] 召回 top_k/阈值按场景调优；记忆神经子代理提示词微调。
- [ ] 阶梯晋升理由与条目跨层去重的人工复查。

## 四、可视化（数据都已落库，随取随画）

- [ ] **记忆时间线**：事件时间轴 + 五层阶梯榜卡片（日/周/月/年/永久）。
- [ ] **内心曲线**：revision 序列的 mood 折线（inner 表）。
- [ ] **当天轨迹时间轴**（day_trajectory）。
- [ ] **记忆图谱**：域→维度→概念 + 事件索引关系的力导图。
- [ ] 控制台 UI 美化（现在的页面朴素）。
- [ ] 日构建进度面板（复用 5090 监视台的样式）。

## 五、工程化

- [ ] PluginApi 纯契约单测再拆一层（ChatCheck 已覆盖内核路径）。
- [ ] 一键启动脚本（宿主 + NapCat + 检查）。
- [ ] 数据备份策略（sqlite 定期快照）。
- [ ] 日志聚合与轮转。

---

**近期约定**：Brain 保持平台无关（平台规则走注入）；可关的是平台与器官，内核组件不可关；先优化后新功能；能力语言统一、身体只填自己的实际值和当前状态、路由永远属于 Brain。
