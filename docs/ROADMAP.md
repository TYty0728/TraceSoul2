# 接下来能做（ROADMAP）

按优先级排列；✅ = 已完成。

**2026-08-19：** 第一波连续感已落地。下面「一、优化与修复」是 QQ 真聊磨合；看见 / 身体场 / 能力分档仍暂缓。

## 一、优化与修复（短期）

- [ ] **表情去重**：移植老 smartemoji 的「近期不重复」短期记忆（同一情绪换着发，不总发同一张）。
- [ ] **表情阈值/候选**：语义兜底阈值与标签匹配权重微调；候选多于一张时引入随机（同分候选随机选）。
- [ ] **消息组装细节**：多个表情连续追加的顺序；文字+图片+语音混排规则；群聊里 @ 与回复（reply 段）的组合。
- [ ] **TTS/生图实测磨合**：待填 api_key（`plugins\qq-tts\plugin.json`、`plugins\qq-imagegen\plugin.json`）；音色/模板/超时第一轮磨合。
- [ ] **说说实测**：真发一条验证 cookie/g_tk；处理发布后的「假失败」（老插件踩过：接口返回非 0 但实际已发出，绝不能重试重复发）；Cookie 过期自愈。
- [ ] **稳定性**：
  - 宿主重启时 NapCat 重连窗口（30s）期间的消息提示；
  - LLM 结构化输出连续失败的自动重试/降级（日构建里已见过两次截断）；
  - 日构建失败天自动重跑（手动补跑命令已可用：`dotnet TraceSoul2.Migrate.dll build --day <日期>`）。
- [ ] **控制台细节**：回合结果里图片路径改成可点击预览；长路径截断；表情包库缩略图浏览页。
- [ ] **多会话**：QQ 收件箱目前单会话路由（回复最后说话的人）；群聊多群、私聊多人并行会话的路由表。
- [ ] **Q版表情库扩充**：往 `plugins\qq-sticker\emojis\<角色ID>\` 加图 + `emoji_index.json` 加条目（可继续用老 WebUI 上传后拷贝）。

## 二、更多感官/平台（外部插件包模式）

- [ ] **语音转文字**（老 `speech_reco`）：QQ 语音消息 → 识别文本进 Brain（OneBot 收 record 段 → 拉文件 → 识别 API）。
- [ ] **QQ 状态/签名**（老 `qqstatus`）：改签名/状态、好友审批。
- [ ] **主动消息**（老 `activemessage`）：定时问候等（配合 `time.schedule`）。
- [ ] **说说读取**：读她/自己的最新说说进上下文（老 `get_feeds` 逻辑已可移植）。
- [ ] **其它平台适配器**：Discord / Telegram 等，按 OneBot 模式各写一个适配器 + 插件包。
- [ ] **生图升级**：模板参数化（场景模板可选）、多尺寸。
- [ ] **能力分级命名（暂缓）**：等真有第二个身体（机器人/摄像头）再给「本地能干多少活」正式分档命名（只转发 / 先筛一遍 / 能认模式 / 自己干完），落进贡献目录；现在两个平台都用不上（见 [定位文档](PLATFORM_SENSORY_POSITIONING.md)）。
- [ ] **动态状态降级（暂缓）**：身体随时说实话——被挡/带宽掉/算力紧时实时降级自己；现在只有「有/没有」两种状态。
- [ ] **安全强制项（暂缓）**：保命开关（急停等）不许身体说「没有」，框架硬性校验；等有身体再实现。

## 三、记忆深化

- [ ] 浸染质量抽查（细节条目的第一人称一致性）。
- [ ] 召回 top_k/阈值按场景调优；记忆神经子代理提示词微调。
- [ ] 阶梯晋升理由与条目跨层去重的人工复查。

## 四、可视化（数据都已落库，随取随画）

- [ ] **记忆时间线**：事件时间轴 + 五层阶梯榜卡片（日/周/月/年/永久）。
- [ ] **内心曲线**：revision 序列的 mood 折线（inner 表）。
- [ ] **当天轨迹时间轴**（day_trajectory）。
- [ ] **记忆图谱**：域→维度→概念 + 事件索引关系的力导图。
- [ ] 控制台 UI 美化（现在的页面朴素）。
- [ ] 日构建进度面板（复用 5090 监视台的样式）。

## 五、工程化

- [ ] 单元测试（PluginApi 纯契约可先行）。
- [ ] 一键启动脚本（宿主 + NapCat + 检查）。
- [ ] 数据备份策略（三套 sqlite 的定期快照）。
- [ ] 日志聚合与轮转。

---

**近期约定**：Brain 保持平台无关（平台规则走注入）；插件在主项目之外、可插拔；先优化后新功能；能力语言统一、身体只填自己的实际值和当前状态、路由永远属于 Brain（分级命名等有第二个身体再定）。
