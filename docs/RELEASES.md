# 版本与发布

## 0.1.15（2026-09-26）

- 普通对话改为 Agent 循环：资料充分时一次生成直接回复；需要记忆、工具或外部信息时，执行能力并根据实际结果继续生成。仅在明确需要加工正文时调用表达模型，保留后台记忆整理、身份复盘、心跳及图片路由。
- PluginApi 升至 1.5.0，新增身体目标、行动组、执行 ID、持续执行回执与取消契约。支持语音或动作独立完成回应；回执区分受理、执行中、实际完成及取消请求，防止迟到回执复活终态。
- 修复 QQ 系统错误/警告通知偶尔显示 `<>f__AnonymousType…[]` 的问题：OneBot 的 HTTP/WS 共用标准 JSON 序列化，消息段数组和嵌套对象保持结构，正文不再被替换为 .NET 类型名。内置 OneBot 插件升至 1.4.2。

本版不包含未完成的认知网改造。执行账本为进程内状态；机器人/Live2D 驱动、原生流式音频、精确跨设备同步以及真实模型/设备体验仍待后续实现或验收。安装新版不代表这些外部能力已经可用。

验证：独立检出目录的 Release 全解决方案构建、完整 ChatCheck、Prompt 布局、86 项媒体/通话检查、CoreCheck、更新器自检、UpdateCheck、Python 与 WebUI 更新/日构建检查全部通过；Windows 发布包生成、七个官方插件清单及 SHA-256 校验通过。真实 Docker 升级由发布 CI 验证。系统通知回归直接检查最终 HTTP/WS JSON，覆盖消息段、正文换行、会话 ID、echo 转义及原有通知去重，不向真人发送测试消息。

## 0.1.14（2026-09-21）

- 修复实时状态“活跃事件”被历史补构建挤占的问题：按事件发生时间展示最近 8 条，补跑旧日不会把较新日期藏到列表后面。
- 复盘使用独立温度，旧配置默认 0.2，不再继承聊天高温；WebUI「大脑 · LLM」可设置 0～1，持久化后供身份复盘与下次日构建使用。聊天参数保持原值，模型固定参数兼容仍由协议层处理。
- 日构建预检显示复盘温度和服务器模型覆盖提示；记忆页可直达复盘设置，修改槽位或温度后旧检查失效，须重新检查再开始。
- 结构化输出失败时向模型反馈真实校验类别与 JSON 位置，要求完整纠正；连续两次失败分别保留安全诊断，仍只允许一次结构纠正，日构建失败继续暂停。

PluginApi 维持 1.4.0，官方插件版本不变。不包含工作区认知网改造；不清理、回滚或自动重跑用户已有记忆。模型输出仍存在不确定性，低温和纠正不能保证任意供应商永不返回错误 JSON。

验证：独立检出目录的 Release 全解决方案构建、完整 ChatCheck、Prompt 布局、CoreCheck、更新器自检、UpdateCheck、Python 更新器检查及日构建/更新 WebUI 检查通过。新增回环请求验证复盘默认低温、零温度持久化、环境模型覆盖与聊天参数隔离；模拟输出验证位置纠正、两次失败留痕及请求有界。未调用真实模型或修改服务器角色数据，本机无 Docker，真实容器升级由发布 CI 验证。

## 0.1.13（2026-09-20）

- 新增媒体理解官方插件：识别 B站/抖音分享链接，受限获取视频，校验大小与时长，抽取时间点画面和平台字幕，再调用多模态槽回答问题。结果区分页面元数据、画面与字幕证据；没有字幕时不推测对白，临时视频和帧在结束后清理。
- 新增实时通话官方插件：Unity 或 QQ 音频网关提交最终 ASR 转写，现有心智生成回复后推送客户端 TTS。支持会话归属、请求重放、打断代次、过期输出丢弃和播放回执；未播放回复不进语义记忆，打断只保留已播放前缀。
- PluginApi 升至 1.4.0，加入媒体证据、实时会话和播放回执共享契约；发布包新增 `media-understanding` 与 `realtime-call` 两个官方插件。

86 项媒体/通话离线检查、4 项 QQ 桥诊断检查、完整 ChatCheck、Prompt 布局、Host 与全部官方插件构建通过。真实 B站/抖音模型调用、Linux QQ 来电双向音频、Unity 客户端、GPT Live、主动拨出和视频仍待对应外部环境实现或验收；本版本不包含工作区中的认知网改造。版本提交不等于服务器部署。

## 0.1.12（2026-09-20）

- 修复表情包无匹配导致后续 QQ 消息只入队、不再回复的问题。表情发送失败按 WARNING 报告；对话、模型和其它能力错误按 ERROR 报告，不再暂停后续聊天。
- 启动时将旧版 `dialogue` / `provider:*` 停聊记录转换为非阻塞报告，保留错误和通知去重状态；无需手动解除旧停聊标记。失败对话不自动重发。
- QQ 通知和 WebUI 区分警告、错误报告与后台任务暂停。同一未处理报告只通知一次，回执不明不自动重发；日构建失败停止补跑及单次模型请求上限继续保留。

PluginApi 维持 1.3.0，空间插件 1.4.0，相机插件 2.2.1；不包含工作区中的媒体/实时通话开发与认知网改造。待提交源码的独立副本通过 Host/Migration/ChatCheck 构建、完整 ChatCheck、Prompt 布局及日构建 WebUI 检查，覆盖表情警告、后续模型调用、旧数据库升级、通知去重与日构建保护。版本提交不等于服务器部署或发布 Release。

## 0.1.11（2026-09-20）

- 增加持久化错误保护：模型请求耗尽有限重试后暂停供应商通道，余额/鉴权错误直接停止，重启不会解除暂停。单次客户端调用最多三次 HTTP 请求，包含空响应、recitation 和兼容回退；结构化校验仍允许一次纠正调用。
- QQ 在线且已有会话时发送去重的系统 ERROR 通知；离线保留待通知状态，发送失败或回执不明不自动重发。通知不调用模型、不进入角色记忆。
- 日构建先只读检查历史失败、未完成日期、复盘配置及暂停状态，再按旧日到目标日执行。整批互斥，旧日失败立即停止；旧版遗留失败不会在升级启动时自动重复收费。
- 复盘失败、超时或中断后持久化暂停，细节生成首错取消并行批次剩余任务。WebUI「记忆 → 错误保护」提供查看和手动恢复；处理阻塞后须检查并显式启动历史队列。
- 每次日构建的阶段、错误和完成状态分别保存，首次升级保留旧状态表中尚存的错误，后续失败不再覆盖先前记录。

PluginApi 维持 1.3.0，空间插件 1.4.0，相机插件 2.2.1。不包含未完成的认知网改造。Host、Migration、ChatCheck 构建与离线回归通过，覆盖请求限次、跨重启暂停、QQ 通知去重、并行首错停止、历史优先队列及 WebUI 检查流程。已有部分记忆不自动回滚，旧版已覆盖的首次报错无法恢复；真实 QQ 通知与云端安装后表现仍待验证。

## 0.1.10（2026-09-20）

- 修复同一 QQ 图片的 CDN 地址和缓存名被重复加载、向识图模型发送两份图片的问题，按实际图片内容去重。
- 控制台在宿主内存保留最近 300 条日志，打开、刷新或重连时回放；移除相机请求正文及大段图片编码，限制单条日志长度。宿主重启后历史清空。
- 说说增加具体素材与自身感想，并可自行决定配图；中枢安排相机生成、空间上传，再把图文一次发布。生图/上传失败时不发布残缺内容，发布请求不自动重试。
- 清醒的空闲保留下次心跳，漏填时兜底 240 分钟；普通聊天中的显式联系时间和方向也会采用。睡下或用户关闭心跳仍停止安排，醒来后可以继续选择安静。
- 夜间余温增加全天轨迹与事件素材，展开自身感受，不再默认以守夜、掖被子等动作代替想法。

PluginApi 维持 1.3.0；空间插件 1.4.0，相机插件 2.2.1。本版不包含未完成的认知网及其探测工具。验证覆盖图片去重、日志回放、心跳持久化、图文发布/失败/取消/旧插件兼容，完整 ChatCheck 与 Prompt 布局检查通过，Host 及受影响插件构建通过。图文说说尚未向真实空间试发，云端 Gemini 中转 HTTP 500 也尚未复测，不能将图片去重修复等同于中转故障已解决。

## 0.1.9（2026-09-17）

- 修复入站识图从 CDN 下载图片时沿用错误 MIME 的问题：PNG/JPEG/GIF/WebP 按文件签名识别，内联 Base64 同样规范类型和检查大小。
- 识图请求日志增加客户端类型、模型名、图片数量和 MIME；失败时记录脱敏后的错误原因，便于排查云端接口异常。
- 识图失败或未配置不再显示“识图完成”；正常取消请求向上传播。
- 新增回环 HTTP 回归，覆盖 NapCat 路径跨容器不可读时使用 CDN、错误 MIME、错误页面、Gemini 原生/OpenAI 兼容请求及错误日志脱敏。

PluginApi 维持 1.3.0。本版不包含尚未完成的认知网改动及本机夜间余温提示调整。离线验证通过；云端故障是否恢复仍需更新后使用原模型实测。

## 0.1.8（2026-09-12）

- QQ 收件箱按机器人、会话和消息 ID 去重，避免重连重放或并发投递重复触发回复。
- 入站轮次的文字和延迟照片沿用原会话，避免后续消息改变收件人。
- 相机把真实发图回执和后续文字相处次数提供给心智，支持自然主动分享；不按固定轮数强制发图。
- 明确索图只在最终回应补图；修复拒绝收图被关键词误触发，以及发送失败误报成功。图片表情单独发送时仍按表情记录。
- 新增项目知识库与开发入口；共享插件 SDK 升至 1.3.0，增加心智动态提示钩子。

离线验证覆盖并发入站、会话路由、图片反馈和 Prompt 分层；真实 QQ 分享频率仍需实际聊天观察。此版本不包含本机另存的夜间余温提示调整。

产品版本只由 `Tools/Directory.Build.props` 的 `TraceSoul2Version` 决定。日常修改和推送不改版本；只有决定集成一个正式版本时才执行：

```powershell
pwsh scripts/Set-Version.ps1 0.2.0
git add Tools/Directory.Build.props
git commit -m "release: 0.2.0"
git tag v0.2.0
git push origin main --tags
```

推送 `v*` 标签后，GitHub Actions 会验证标签和源码版本一致，构建 Windows x64、Linux x64 与 Linux arm64 发布包，生成 SHA-256，并创建 GitHub Release。普通 commit 不触发产品发布，也不会让已安装电脑看到更新。

## 已安装电脑的一键更新

WebUI「系统更新」填写一次 `owner/repository`。检查更新只读取该仓库最新的正式 Release；点击安装后会：

1. 按当前系统下载 `tracesoul2-<runtime>-v<版本>.zip` 和同名 `.sha256`；
2. 校验 SHA-256 并在家目录 `updates/` 中解包；
3. 从软件目录外启动更新器，退出旧宿主；
4. 整体替换应用目录并重启；
5. 保留旧应用目录作为可回滚备份。

安装期间 WebUI 每秒读取进度，显示下载字节、百分比以及校验、解压、准备替换和重启等阶段。浏览器刷新或反向代理断开不会取消服务器上的下载；相同进度会按阶段和约每 10% 记录到 `Data/updates/update.log`。

从 v0.1.7 起，安装接口立即返回、由后台任务下载；安装包与 SHA-256 文件都优先使用 GitHub API 资产接口（`Accept: application/octet-stream`），由 GitHub 重定向到官方资产域名，失败时尝试官方 Release 地址。使用 HTTP/1.1、20 秒连接超时、45 秒无数据超时和最多 6 次重试，不因总下载耗时长而中止。部分文件按仓库、资产 ID、大小与更新时间隔离保存在 `updates/downloads/`，再次安装可断点续传。服务器不支持 Range 时重新下载，不会把完整响应拼接到旧文件后；所有缓存仍必须通过 SHA-256 校验才能安装。

网络至少需要能访问 `api.github.com` 和 `release-assets.githubusercontent.com`。若这些官方域名也不可达，仍需修复服务器网络；更新器不会使用第三方镜像或关闭 TLS 校验。99% 表示等待更新器与重启，只有新版宿主上线才确认成功。

旧版下载通道无法联网时，先用仓库内 `scripts/update-server.py` 引导升级一次。它仅用于默认 Docker 目录布局，验证挂载路径、自动识别 CPU、续传并校验官方包，然后启动真正的事务式更新器。确认更新器通过准备检查后才让旧宿主退出；保留旧程序、官方插件备份和全部持久化数据。

`souls/`、`plugins_data/` 和 `home.json` 都在应用目录外，不会被更新包覆盖。仓库内维护的官方插件代码包会随 Release 一起升级：更新器先备份旧插件包，再替换 DLL、清单和默认资源；失败时与 App 一起回滚。独立安装的第三方插件不受影响。

Docker 部署使用宿主机 `runtime/App`、`runtime/Plugins` 和 `runtime/plugins_data`，因此也是同一套更新流程：容器内的外置更新器替换 `App`、升级 Release 声明的官方插件包，入口 supervisor 随后启动新版本，不需要挂载 Docker Socket。`runtime/plugins_data` 始终保留。

当仓库可以公开读取、但还没有正式 Release 时，GitHub 的 `releases/latest` 接口也会返回 HTTP 404。更新页会进一步检查仓库本身并明确提示“还没有正式 Release”；普通 `main` 提交不会成为可安装更新。

更新检查不保存 GitHub Token，因此 Release 仓库必须允许匿名读取。若源码需要保持私有，可以把构建产物发布到另一个公开的 Release 仓库，并在 WebUI 填那个仓库。

## 本地生成发布包

```powershell
pwsh scripts/Publish-Release.ps1
```

输出在 `artifacts/<版本>/`。软件 ZIP 同时包含 Host、每日迁移管线、外置更新器与仓库内维护的官方插件包；PluginApi 另外生成独立 `.nupkg`。ONNX 模型由 Git LFS 管理，生成发布包前必须已拉取完整文件。
