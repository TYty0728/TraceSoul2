# 媒体理解与实时通话实施 TODO

目标：两个可独立启停的插件，媒体理解首批支持 B站、抖音；实时通话优先 QQ 原生电话，保留 Unity 客户端，后续接实时视频。沿用 TraceSoul2 的人格、记忆和心智决策。

## 1. 可验证的基础（已完成，2026-09-20）

- [x] 核对旧分享、通话、语音识别插件的实际源码能力。
- [x] 确认 QQ AV 桥的控制协议、音频设备要求及部署差异，提供只读诊断入口。
- [x] 定义媒体证据和实时会话共享契约；区分页面信息、画面、语音和已播放回执。
- [x] 实现 B站/抖音来源识别与链接规范化，拒绝把页面元信息标成已理解视频。
- [x] 实现通话会话生命周期、连接归属、序号去重、打断代次与断连清理。
- [x] 提供插件包、默认关闭的未配置端点和离线回归，构建 Host 与受影响插件。

验收：不调用真实模型、不拨打真人电话，模拟两个客户端不能串会话，打断后旧回复失效；链接识别覆盖短链、分享文字与伪造域名。

## 2. 媒体理解完整链路

- [x] 可访问媒体获取实现：B站、抖音交给 `yt-dlp` 的站点适配；限制下载大小、时长、超时、输出数量和临时文件生命周期。
- [x] 视频均匀抽帧与 VTT/SRT 字幕时间戳对齐；按用户问题把画面和可用字幕交给模型。
- [x] 复用多模态识图槽；输出页面元数据、画面、字幕证据、覆盖范围与稳定失败码。
- [ ] 直接音轨 ASR 与按问题选择更细片段；当前无字幕时只理解离散画面。
- [ ] 接入 QQ 入站与工具调用；缓存分析结果以支持后续追问，原始帧不逐条写入主记忆。
- [ ] 测试本地素材与两个平台的真实分享链接，验证只获得封面时不声称看过视频。

## 3. 实时语音完整链路

- [ ] 只读诊断部署环境后，在隔离环境验证 QQ 来电、接听、双向音频和挂断。
- [x] 实现共享转写/TTS 会话语义：外部 ASR 最终文本进入现有心智，回复推送客户端 TTS。
- [ ] 把 QQ AV 桥 PulseAudio 双向音频接到转写/TTS 协议；Unity 客户端实现麦克风、ASR、TTS 与扬声器。
- [ ] 优先验证 GPT-Live client delegation 与现有心智的表达边界；基础后端使用流式 ASR → TraceSoul2 → 流式 TTS。
- [x] 实现会话打断/取消、旧代次输出丢弃、空闲超时、请求重放和断连清理。
- [ ] 客户端实现边听边说、回声处理与断线恢复。
- [x] 通话绑定现有对话；未播放输出不进入语义记忆，完整播放记全文，打断只记已播放前缀。
- [ ] 测量首音延迟、打断延迟、丢包/断线与成本，验收真实中文通话。
- [ ] 单独验证 QQ 主动拨出能力；不把来电接听成功当作拨出已支持。

## 4. 视频与演示

- [ ] Unity 麦克风、扬声器与摄像头客户端，明确前后台运行限制。
- [ ] 实时画面带时间戳；过期帧可丢弃，语音保持优先。
- [ ] 第一条完整演示：分享视频 → 电话讨论片段 → 打断纠正 → 挂断回 QQ 继续聊天。
- [ ] 机器人音视频接口开放后仅增加设备适配，不复制人格和记忆链路。

## 状态与约束

本文件是实施清单，未勾选项目不代表已经实现。QQ AV 桥来自社区项目，Linux QQ/AVSDK 版本兼容、主动拨出和视频能力尚未实机验证。不向真人发送自动验收消息。已有认知网修改保持原样。

用户已确认目标为 **Linux 服务器上的 Docker**。本机没有该服务器的容器连接，因此 QQ 前置检查仅提供脚本，未对远端执行。当前代码可在没有模型密钥的情况下完成全部基础回归。

### 当前交付与验证

- `ExternalPlugins/MediaUnderstanding/`：`media.source.inspect` 识别来源；`media.video.analyze` 受限下载、校验、抽帧、读取平台字幕并调用多模态槽。没有字幕时不会推测对白，尚不直接识别音轨。
- `ExternalPlugins/RealtimeCall/`：鉴权 WebSocket、最终转写入口、客户端 TTS 输出、时长/播放回执和中断语义；协议和 Docker 桥接说明见该目录 README。
- `src/TraceSoul2/Data/MediaRealtimeData.cs`：共享纯数据，加入 PluginApi 1.4.0 显式源清单；旧版 Host 不能直接安装新插件。
- `scripts/probe_qq_call_bridge.py`：在 QQ 容器内运行的只读诊断，不复制上游桥源码、不安装或修改 QQ。检查通过也不声称通话已验证。
- `Tools/MediaRealtimeCheck`：86 项检查通过，包含来源解析、抽帧采样、字幕解析、临时资源清理、无 shell 参数传递、输出上限/进程超时、独立回环 WebSocket 鉴权与限制、会话隔离、最终转写、服务端推送、完整/部分播放记忆、打断和断连清理。
- Python 4 项诊断检查通过：回环地址边界、脱敏、缺令牌状态、拒绝重定向。
- Host、新两个插件与现有 GameSession/QqTts/QqStatus/QqQzone/QqImageGen 构建通过；完整 ChatCheck 与 Prompt 布局回归通过。
- 当前外部验证缺口：本机没有 `yt-dlp`/FFmpeg，也没有目标 Linux QQ 容器连接，因此尚未跑真实 B站/抖音链接、真实模型、QQ 来电/音频和 Unity 麦克风。QQ/Unity 仍需音频网关把 ASR/TTS 接到已完成的转写协议；GPT Live、主动拨出和视频尚未实现。
