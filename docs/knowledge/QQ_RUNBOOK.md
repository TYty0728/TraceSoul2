# QQ 回复与照片排障

## 多次回复

先用日志 `TraceId` 和入站 `event=message_id` 区分：

| 现象 | 检查点 |
|---|---|
| 同一个 `message_id` 多次进入收件箱 | OneBot 重放或多 WS 投递；本次已在入队前去重，日志为“QQ 重复消息已忽略” |
| 不同 ID、正文一样 | 可能是用户实际重复发送；不按正文吞消息 |
| 一次输入先说“等一下”，稍后带结果回来 | `KernelLogic` 外出分支设计上有等待与最终回应；不是普通轮重放 |
| 文字之后还有图、语音 | 附加表达；文字与表情合并，照片轮后单独发 |
| 用户回复附近又收到主动消息 | 比对心跳/时间触发的 TraceId；不能仅凭时间近认定为重复发送 |
| 消息或延迟照片发错聊天 | 优先检查触发 Moment 的会话载荷；当前已改为旧轮沿用原会话 |

去重是进程内的 30 分钟 / 4096 条有界缓存，跨进程重启或超出窗口不保证去重。没有有效 ID 时继续接收。不要盲目重试发送动作：超时可能发生在 QQ 已收到了消息之后。

## 收到图片但看不清内容（2026-09-17）

阶段日志由 `SoulRuntime.Emit` 发到控制台网页 `/events` 实时事件流，不写 Docker stdout；`docker logs | grep 识图` 无输出不能证明未执行识图。2026-09-19 工作区修复后，即使网页未打开，Host 也会在内存保留最近 300 条事件；打开/刷新/重连时回放，再接实时事件。Host 重启后历史清空，不是磁盘日志。

- `get_image｜unreadable-file,qq-cdn` 表示协议同时返回其他容器的文件路径和 CDN 地址；还会继续尝试 CDN，不能仅凭该行归因共享目录。
- `识图模型失败` 表示已取得非空图片候选并进入模型调用，不代表图片格式或供应商请求有效。旧版只记录异常类型；旧版 `识图完成｜chars=24` 实际可能是固定失败提示。
- 本次修复：入站 PNG/JPEG/GIF/WebP 按文件签名规范 MIME，覆盖 CDN 返回的 `application/octet-stream` 或错误类型；内联 Base64 同样规范并检查大小。无法识别且没有图片 MIME 的数据不再默认伪装 JPEG。
- 新日志显示模型客户端类型、模型名、图片数量和 MIME；模型失败保留脱敏、截断后的异常原因。已知 API Key、URL、Bearer 和内联图片数据做脱敏；排障仍只分享必要阶段行。失败和未配置分别记为“识图未成功”“识图未配置”，不再统称完成。调用方取消正常向上传播。

验证：新增 `Tools/ChatCheck/VisionTransportChecks.cs` 使用回环 HTTP 模拟 CDN 和模型服务，覆盖跨容器路径不可读、二进制/错误 MIME、错误页面、Gemini 原生与 OpenAI 兼容的实际请求体、HTTP 400 错误可见性与脱敏。`--vision`、完整 ChatCheck、`--prompt-layout` 通过；Host/ChatCheck 构建通过。没有调用真实模型或发送 QQ 消息。

本机 Gemini 已由用户实测成功，识图描述进入了后续回复。云端旧日志只显示 `InvalidOperationException`，没有状态码和具体错误；MIME 是源码中已修复的确定缺陷，不能据此宣布此次云端故障已解决。更新到 0.1.9 后保留云端原模型复测，查看具体错误。本机源码修复不会自动更新云端容器。

### 2026-09-19 后续定位

用户提供的新日志显示云端已取得两份 JPEG，并通过 `DeepSeekClientManager` 调用 Gemini 的 OpenAI 兼容中转；上游返回 HTTP 500 `upstream error: do request failed`，退避重试三次耗时约 32 秒。用户随后换 Kimi 识图成功。此证据将当次失败定位到模型调用阶段，不能归因于 NapCat 文件不可读，也不能证明模型本身无视觉能力。

确定修复：`OneBotPlatformAdapter` 会保留同一图片的 URL、缓存名等备用来源，旧 `VisionLogic` 将每个成功来源都加入请求，造成一张图重复上传。现在按图片字节的 SHA-256 去重，在独立图片计数达到四张之前继续加载；URL/Base64 标识采用区分大小写的比较。请求日志新增图片字节总数，去重日志仅报数量，不报图片内容。ChatCheck 用单个 QQ 图片段、两个不同 CDN 来源和跨容器路径模拟，核验两种协议最终都只有一份图片，也覆盖多个别名不会挤掉后续不同图片。

重复图片会增加请求大小与输入，但尚无证据证明其导致本次中转 500。更新云端后应看单图请求 `images=1`，并在相同供应商、模型、API 类型和图片下复测；若仍 500，继续核对云端供应商通道与上游错误。未自动切换模型或 API 类型，未访问真实模型做测试。

## 日构建失败的离线排查（2026-09-20）

- 先在数据包副本上读取 `migration.sqlite3` 的 `migration_review_state`，核对 `DayKey`、`Status`、`Error` 和更新时间；主库有 WAL 时连同 WAL 一起复制。不要启动真实 Host 或迁移构建来做只读诊断。
- 日构建由 `MigrationContext` 优先读取 `TRACESOUL2_MIGRATION_PROVIDER` / `TRACESOUL2_MIGRATION_MODEL` 环境覆盖，否则使用 `llm-providers.json` 的 `review` 槽，未配置才回落到聊天。聊天或识图切换供应商不会覆盖独立复盘槽。数据包不能证明服务器环境变量的实际值。
- 本次数据包中，最近两天构建状态均为 `failed`，最新错误为 `API 402: Insufficient Balance`；主库与迁移库 `PRAGMA quick_check` 均通过。应检查实际复盘供应商账户余额或更换复盘槽，而非重建数据库。未连接服务器或调用真实模型，未修改原数据包。
- `DayBuilder` 仅在全日成功后标记完成，失败状态只保存最新错误。`replay_call_log` 记录成功阶段，能证明重复执行，不能还原被覆盖的全部历史错误。本次副本确认存在同一天重复观察及未完成的事件条目。
- 0.1.10 及此前版本失败后五分钟补跑，旧日失败会阻止自动推进后续日期。下面的错误保护修复改为失败后持久化暂停。构建并非整日事务，也未实现阶段断点续跑，失败前写入的条目仍可能保留，不能把重跑视为完全无重复副作用。不要直接删除未完成日期的聊天或整库记忆。

### 再次失败的数据包核对（2026-09-21）

- 只读打开下载包，使用 SQLite backup API 制作包含 WAL 已提交内容的诊断副本；主库、迁移库、向量库、错误保护库的 `PRAGMA quick_check` 均为 `ok`，主库 Moment 的非空 PayloadJson 均为合法 JSON。未启动真实 Host、未调用模型、未修改原包配置或业务记录。
- 9 月 18、19 日均已标记 `done`；本次失败的是 9 月 20 日，执行时间为北京时间 9 月 21 日 04:00:25～04:01:53。尝试表记录阶段为“事件观察（第 1 批）”，错误为模型连续两次返回不可用的结构化输出；首错为 JSON 值后出现非法冒号，`LineNumber: 2 / BytePositionInLine: 1015`。这是模型输出解析位置，不是聊天记录行号；此次记录不是此前的 HTTP 402。
- 9 月 20 日没有成功的 `replay_call_log`，119 条待构建 Moment 仍为 `live`。失败发生在第一批输出通过校验之前；不能将状态表的默认 `MomentCount=0` 解读为当天没有聊天。
- 数据包复盘槽为 `default / deepseek-flash`，对应供应商温度为 `1.6`、输出上限为 `8192`，Key 非空。当前源码复盘只强制关思考，仍继承供应商温度；聊天/思考槽切换不会替换复盘槽。高温度是可调整因素，不能仅凭它认定 JSON 错误根因；服务器环境覆盖和实际请求参数仍需运行日志核实。
- `daily` 暂停记录仍在，通知已尝试。调整实际复盘配置后，需在 WebUI 恢复日构建、重新检查并显式执行；单纯更新或重启不会清除此暂停。修复响应、第一份错误响应都未保存在数据包中，当前异常外层只保存首错，无法还原具体错误字段或第二次失败细节。
- 保护库仍只有早期五列，缺少当前 `FailureProtection` 初始化会添加的 `NonBlocking/Severity`。这份目录未体现晚间错误报告修订的数据库升级；实际运行包版本需另行核对，不能仅凭用户已更新推定最新 Host 正在使用该目录。
- 实时状态“活跃事件”另有展示排序问题：原先按 `UpdatedUnixMs` 取前 8 条。该包先完成 19 日，再补跑 18 日，复现结果为前 8 条全部属于 18 日；19 日实际存在 42 个事件索引及 51 条带细节的关联条目。Host 展示现按 `TimeUnixMs` 降序、写入时间作为次序取 8 条；同包验证结果为 19 日。仅改变状态展示，不改变共享存储查询与召回规则。20 日第一批观察失败，没有新增日构建事件，仍需处理上述复盘故障。
- 展示修复验证：诊断副本分别按新旧查询排序核对前 8 条日期，Host 构建 0 警告、0 错误，`git diff --check` 通过；未部署服务器，未启动真实 Host。

### 错误通知与消耗保护（2026-09-20，0.1.12 修订）

2026-09-26，0.1.15 修复：系统通知如果显示 `<>f__AnonymousType…[]`，原因是 OneBot 传输层曾对匿名消息段数组调用 `ToString()`，类型名代替了通知正文。HTTP/WS 现共用 `JsonSerializer` 序列化完整请求，保留数组与嵌套对象。回归通过实际通知生成入口取得参数，再校验最终传输 JSON 的正文、消息段结构、数值会话 ID 与 echo；ERROR/WARNING、去重及不自动重发规则保持不变。此现象本身不能说明触发通知的上游错误已解决，升级后应读取真实错误正文定位。

2026-09-21 / 0.1.14 补丁：复盘现在使用 `llm-providers.json` 顶层可选 `reviewTemperature`，未设置时为 0.2；不修改供应商聊天温度。`GET/PUT /providers/review-settings` 与 WebUI「大脑 · LLM → 复盘专用温度」支持 0～1（含 0），模型兼容层仍可采用固定温度。Host 身份复盘、日构建及其显式供应商覆盖使用同一设置；已运行的子进程在下一次启动生效。预检展示配置温度与环境覆盖来源。格式纠正改为反馈实际语法/类型位置或必填字段错误，外层异常保留首错和纠正后的错误，JSON 诊断不持久化生成字段名或正文；最多一次纠正与原有请求上限不变。失败暂停须通过 WebUI 手动恢复、检查、开始，升级不会自动重新调用付费模型。

补丁验证：新增 `ReviewOutputChecks` 使用回环 HTTP 检查实际请求温度、旧配置默认值、零值持久化、显式模型覆盖、聊天参数不变和非法参数拒绝；使用模拟模型验证 JSON 定位纠正及两次错误留存。扩展日构建 WebUI 检查覆盖保存复盘参数不启动构建、旧计划失效和环境覆盖提示。独立检出的全解决方案 Release 构建、完整 ChatCheck、Prompt 布局、CoreCheck、更新器自检、UpdateCheck 与 Python/Node 更新检查均通过。没有真实模型调用或线上部署；本机无 Docker，容器升级留待发布 CI。

发布完成补记：`v0.1.14` / `b147485` 已发布，GitHub Release 流程与真实 Docker 升级/数据保留检查全部通过。匿名 `releases/latest` 返回新版本，Windows x64、Linux x64/ARM64 的安装包与 SHA-256 均可公开读取，校验文件与 GitHub 资产 digest 一致。用户通过 WebUI「系统更新」安装后，仍需在错误保护恢复日构建、重新检查并开始；未替用户安装服务器或发起真实模型复盘。

- `FailureProtection` 使用角色目录的独立 `failure-protection.sqlite3` 保存报告与后台任务暂停状态。2026-09-20 晚间修订：模型调用失败仅记录 `ERROR`，不暂停供应商或后续聊天；单次请求仍有重试上限。启动时自动把旧版 `dialogue` / `provider:*` 暂停转换为不阻塞的报告，保留原因和通知去重状态，无需用户再次手动恢复聊天。
- OpenAI 兼容与 Gemini 原生的单次客户端调用最多发出三次 HTTP 请求，网络重试最多两次。空响应、Gemini recitation、温度兼容回退也共用该上限。余额不足及鉴权错误直接失败，不重试。结构化内容验证仍可进行一次纠正调用，因此单个业务步骤不等于只请求三次。
- 日构建执行前写入运行占位；失败、三十分钟超时或进程中断后保持暂停，不再每五分钟整日重跑。宿主正常退出会终止其启动的复盘子进程，包括手动触发的构建。复盘细节阶段的四路并行首个失败会取消在途调用及等待队列，避免其余条目继续花费；保留首个错误。
- 对话轮次异常或返回失败能力结果只生成报告，不设置停聊开关；失败轮不自动重新执行，后续新消息正常取出处理。`qq.sticker.send` 失败（包括没有匹配表情）只报告 `WARNING`，同轮其它失败仍报告 `ERROR`。本地、QQ 后台和夜间余温共用能力结果分类。后台收件箱不是跨重启持久化队列。
- 独立通知任务在 QQ 在线且已有有效会话时，向最近记住的 QQ 私聊或群会话直接发送 `【系统 ERROR】` / `【系统 WARNING】`。混合报告逐条标明级别，只有真正暂停的后台任务才标记“此任务已暂停”。仅报告安全错误类别，不透传上游正文、密钥或私聊；不调用模型，不进入角色记忆，也不经过普通文字暂存。离线时保留待通知状态，上线后再尝试。
- 每个未处理报告在发送前原子占位，只尝试通知一次；发送超时或回执不明也不自动重发，避免报错风暴。WebUI「记忆 → 错误保护」区分“仅报告，不暂停对话”和“任务已暂停”；报告可清除，日构建暂停需处理后手动恢复。若有历史构建失败，恢复后仍需点击日构建检查并显式开始；按旧日优先继续，不会回滚已有部分产物。
- 离线验证入口：`ChatCheck --failure-protection` 覆盖表情 WARNING、其它 ERROR 不停聊、模型恢复后无需手动解锁、旧结构数据库升级、持久化、调用方取消、离线/上线通知、并发去重、回执失败不重发、构建中断、并行队列首错停止，以及两种模型协议的 HTTP 402/500、空响应、recitation 和温度兼容请求上限。未连接真实模型、未向真实 QQ 发送验收通知；云端尚未更新。

晚间修订验证：完整 ChatCheck、错误报告专用回归和日构建 WebUI 检查通过；Host、Migration、ChatCheck 构建均为 0 警告、0 错误，`git diff --check` 通过。仅更新本机构建产物，未启动真实 Host 或发送 QQ 测试消息，未部署云端。

#### 旧版服务器临时解除停聊

0.1.11 的暂停记录在当前角色目录的 `failure-protection.sqlite3`，与 `llm-providers.json` 同级。默认 Docker 宿主机位置为 `runtime/Data/souls/<角色>/failure-protection.sqlite3`，容器内为 `/opt/tracesoul2/Data/souls/<角色>/failure-protection.sqlite3`。角色由 `runtime/Data/home.json` 的 `activeSoul` 指定；若使用 `TRACESOUL2_DATA` 或调试分支，以实际运行的 `dataDirectory` 为准。重启和更换模型不会清除该数据库记录。

最快的方法是在 WebUI「记忆 → 错误保护」恢复“对话或附加能力”和相关“语言模型通道”。若直接操作文件，先停止对应 Host/容器，使用 SQLite 备份整个保护库，再执行以下 SQL，仅移除对话与供应商停聊记录，保留 `daily` 等后台任务状态：

```sql
DELETE FROM failure_stops WHERE Key = 'dialogue' OR Key LIKE 'provider:%';
```

随后重新启动。不要删除整个保护库或角色主库；旧版再次遇到表情失败仍可能停聊，升级到 0.1.12 才会移除触发逻辑。只更新源码或重新启动旧容器不会替换已安装的 `runtime/App`，须确认实际运行的版本。

### 日构建预检与历史优先队列（2026-09-20，0.1.11 集成）

- WebUI 日构建按钮先调用 `GET /memory/daily-status`，只读检查主库待归档日期、迁移库 `failed/running` 状态、复盘槽（含服务器环境覆盖）、缺 Key、程序包可用性及暂停记录。此检查不调用模型，不能提前验证上游实时余额或网络是否恢复。
- 页面列出历史错误、保留下来的失败阶段、执行日期顺序及阻塞原因。先处理配置/余额并解除对应暂停，再点击「处理历史未完成日期，再构建目标日」。改日期后旧检查失效；执行中不能重复启动。
- `POST /memory/daily-run` 在后端重新检查，合并所有不晚于目标日的待归档日期、历史未完成记录和目标日，排除已完成日期。即使失败日没有 live Moment，也不能漏掉。只接受已经结束的记忆日；整批由同一门闩保护，手动与自动队列不能交错。
- 统一执行器按日期从旧到新运行，旧日非零退出或缺少 `done` 标记时立即停止，禁止继续后续日期。发现旧版遗留失败时，自动调度只报告并暂停，不能因升级启动自动重复收费；用户处理后从 WebUI 显式启动历史队列。
- 新增 `migration_review_attempts` 独立保存每次尝试、当前阶段、错误和结束状态。首次升级重跑前保留旧状态表中尚存的最后错误；此后新的额度错误不能覆盖之前的格式/阶段错误。此前已被旧版本覆盖的最初报错无法恢复。异常中断的运行记录在下一次尝试时标为 interrupted；成功不会删除之前失败记录。
- 此次下载包的两个最近失败日只保留了 HTTP 402；成功调用记录不包含早期失败堆栈，debug-active 数据也早于此次故障，无法确定最初报错。修改不包含自动清理/回滚部分记忆或自动修复模型账户。
- 回归：`ChatCheck --failure-protection` 包含旧日无 Moment、旧日失败阻断后续、完成日去重、只读检查、旧错误导入及多次失败保留；`node scripts/tests/test-daily-build-ui.cjs` 验证先检查、阻塞禁用、显式开始及改日期使计划失效，已接入现有 CI。Host、Migration、ChatCheck 构建通过；未操作真实服务器或发送真实 QQ 消息。

## 日志、说说与夜间余温（2026-09-19 工作区修复）

- `RecentEventLog` 在无订阅时也保留最近 300 条，回放与订阅在同一锁中完成，慢订阅也有界。前端收到连接握手后替换旧视图，避免重连重复。图片 Data URI、长编码串、换行与超长事件在缓存前处理，一条事件只占一行。
- 相机 `log_request_body` 兼容原配置键，但只打印模型及请求长度，不再打印正文或 Base64 的头尾片段。Host 入口另有编码过滤，覆盖其它日志路径。
- 空闲说说改为完整自然的一小段，表达具体心事及自己的感受/疑问，避免空泛意象；保留可以不发、不必有大事的语义。公开内容不照抄私聊及私密信息。空闲素材的内心和当天轨迹预算扩大，不修改空闲抽签或发布闸门。
- 夜间余温加入当天轨迹，事件从全天采样最多八件，每件保留更多上下文；引导解释自己的在意、理解变化或未解疑问，通常一到三小段。取消“她在睡觉”的默认场景，不以守夜、掖被子等动作代替感想；保留无内容可沉默、发送时窗和每日去重。未修改潜意识/身份复盘内部逻辑。

验证：Host、相机及空间插件构建均 0 警告/错误；完整 ChatCheck、`--vision`、`--prompt-layout` 与 `git diff --check` 通过。新增回放、并发、慢订阅、关闭订阅、编码过滤及全天素材回归。文字自然度、云端更新及 Gemini 真机复测尚未验证。

## 图文说说与清醒联系规划（0.1.10）

- 空间准备阶段返回正文与可选 `image_prompt`，不执行发布。中枢 `QzonePublishLogic` 调用相机 `dispatch=generate` 获取图片，再由空间上传并只提交一次图文说说，不额外发 QQ 私聊图片。相机不可用时只生成纯文字草稿；明确选择配图后若生成或上传失败，本次说说不发布。
- 空间使用 `cgi_upload_image` 的说说附件类型上传图片，校验图片字段后填入发布请求的 `richval`、`pic_bo` 等参数；接口格式参照实现者的[上传接口说明](https://github.com/Gu-Heping/onebot-qzone/blob/main/doc/photo-api.md)与[说说接口说明](https://github.com/Gu-Heping/onebot-qzone/blob/main/doc/emotion-api.md)。这是逆向接口，已用模拟 HTTP 验证请求/回包，尚未向真实空间试发。
- 成功、失败或取消后，清理归相机所有的本次临时图片。旧空间插件不会收到 `prepare` 请求，避免把准备误当发布；旧相机不支持 `release` 时不调用该分支。更新时 Host 与空间/相机 DLL 应一起安装。
- 清醒的空闲不再清掉心跳，已安排的心跳到期仍可唤醒。安静且漏填时间时使用 240 分钟兜底；普通聊天中明确填写的时间和方向也会落入调度，不被首次随机范围覆盖。睡下或手动关闭心跳仍清除安排。唤醒只重新考虑联系，并不保证每次发消息。
- 先保存下次心跳，再执行可能耗时的空闲生活动作；说说生成失败不会因此丢掉后续联系时间。已处于旧版空闲且没有任务的角色，在下一轮正常聊天后按新规则安排。

回归：`QzoneMediaChecks` 模拟纯文字、自主配图、固定正文、相机不可用、生图失败、上传失败、取消、发布回包不确定及旧插件；确认图文只发布一次、不额外私聊发图。`HeartbeatContinuityChecks` 使用真实 SQLite 和时间调度插件验证安静时的 0/90/240/480 分钟、普通聊天的显式时间、睡下与关闭心跳。所有网络操作均由模拟器接收。

## 不发照片

按顺序查，避免只改 Prompt：

1. **运行版本**：Host 和已安装 `qq-imagegen` DLL 是否都更新？编译 Host 不会安装外部插件。
2. **可用性**：QQ 已连接、回发开启、相机插件启用、生图供应商/模型/Key 配置齐全；只检查是否配置，不输出 Key。
3. **心智输入**：稳定段有相机说明与 `image` 字段；动态段有“相机此刻”。后者仅在生图器官可用时加入。
4. **选择**：看“心智判断完成”的 `image`。长期为无时，查看真实发图反馈是否存在、上下文是否适合分享。不要用固定概率强制拍照。
5. **映射**：看“外显图片路由”“后台生图已开始”。`qq.image.send` 只接 `file`，生图须走 `qq.imagegen.generate`。
6. **生成**：看画面规划、参考图、请求错误和“生图未成功，不发图”。供应商问题与未选择发图是两类问题。
7. **发送**：看“QQ 图片发送完成”的 status 与“后台图已发出/后台发图未成功”。发送失败不得伪造成功回执。

相机反馈读取当前会话最近 200 条运行事件内的照片回执，统计最近 80 条真实对话内后续的角色回应。没有记录只说明查询窗口没有，不能说从来没拍过。6 次文字回应是重新考虑分享的提醒阈值，**不是出图配额**。分享频率最终由心智模型和具体聊天决定，需要真聊观察。

## 本地验证与应用

在仓库根目录：

```powershell
dotnet build Tools/ChatCheck/ChatCheck.csproj --nologo
dotnet Tools/ChatCheck/bin/Debug/net8.0/ChatCheck.dll
dotnet Tools/ChatCheck/bin/Debug/net8.0/ChatCheck.dll --prompt-layout
dotnet build Tools/Host/TraceSoul2.Host.csproj --nologo
dotnet build ExternalPlugins/QqImageGen/TraceSoul2.Plugin.QqImageGen.csproj --nologo
```

应用时使用实际启动配置解析插件目录，备份并替换 `qq-imagegen/TraceSoul2.Plugin.QqImageGen.dll`，保留原 `plugin.json`、配置和图库。相机使用 PluginApi 1.3.0，与 Host 0.1.8 同批升级；正式 Release 已包含配套文件。

真人收发验收会产生外部消息；离线回归不启动真实 Host。用户自己启动后，观察普通聊天中照片是否自然出现、同一入站是否只处理一次；如仍异常，留取同一 TraceId 的阶段日志，不必导出整份私人聊天。
